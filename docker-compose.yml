services:
  py-mem-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    image: py-mem-mcp:latest
    container_name: py-mem-mcp
    env_file:
      - .env
    ports:
      - "3000:3000"
    restart: unless-stopped
